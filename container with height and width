import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("Container Height & Width Example"),
        ),
        body: Center(
          child: Container(
            height: 150,
            width: 250,
            color: Colors.orange,
            child: Center(
              child: Text(
                "Sized Container",
                style: TextStyle(fontSize: 18, color: Colors.white),
              ),
            ),
          ),
        ),
      ),
    );
  }
}
