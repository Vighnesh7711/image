import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("Image Asset Example"),
        ),
        body: Center(
          child: Image.asset(
            'assets/image.png', // path of image
            width: 200,
            height: 200,
          ),
        ),
      ),
    );
  }
}
